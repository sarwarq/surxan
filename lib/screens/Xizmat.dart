import 'package:flutter/material.dart';

class Xizmat extends StatelessWidget {
  const Xizmat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.grey,);
  }
}