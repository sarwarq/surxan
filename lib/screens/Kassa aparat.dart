import 'package:flutter/material.dart';

class Kassaaparat extends StatelessWidget {
  const Kassaaparat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red,);
  }
}